# Historical fragments and experiment outputs

These files are copied unchanged from [`archives/RL_PADS.zip`](../archives/RL_PADS.zip).

| File | Original member | Status |
| --- | --- | --- |
| `hcn.py` | `RL_PADS/hcn.py` | Incomplete Hybrid Code Network fragment; references undefined imports, flags, and dimensions, and removed `tf.contrib` APIs. It was not imported by the archived runner. Kept for provenance and excluded from the installed package. |
| `experiment_notes.txt` | `RL_PADS/data/All_experiments_details` | Informal historical notes with multiple settings and small-run metrics; configurations/seeds are incomplete. |
| `evaluation_history.csv` | `RL_PADS/save_dir` | Five historical evaluation rows; original experiment configuration and seed are not recorded. |

These historical outputs are not newly reproduced results and should not be
used as evidence that the maintained code reproduces the published tables.
The full original Python source remains available inside the archive.
